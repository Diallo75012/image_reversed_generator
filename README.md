Code to get the inverse of the image without colors.

# installations
Pillow : python3 -m pip install pillow
OpenCv2 : python3 -m pip install opencv-python-headless
Scikit-image : python3 -m pip install scikit-image


 
